# Railway Deployment Configuration
# This file contains deployment settings for Railway

# Python runtime version
python-3.9.18

# Build settings
[build]
  # Automatically install requirements.txt
  builder = "NIXPACKS"

[deploy]
  # Health check endpoint (optional)
  # healthcheck = "/health"
  
  # Restart policy
  restartPolicyType = "ON_FAILURE"
  
[env]
  # Environment variables will be set in Railway dashboard
  # TELEGRAM_BOT_TOKEN will be added via Railway UI